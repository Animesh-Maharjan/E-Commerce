#############################################
# Base Python / Django / ML .gitignore
# Generated for this repository (E-Commerce)
#############################################

# --- Python bytecode & cache ---
__pycache__/
*.py[cod]
*$py.class

# --- Virtual environments ---
env/
venv/
.venv/

# --- Build / packaging ---
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/

# --- Django / runtime artifacts ---
db.sqlite3
*.sqlite3
media/            # User uploaded files (keep out of VCS) 
ml_models/        # Trained ML pipeline artifacts (joblib / pkl)
staticfiles/      # Collected static root (use collectstatic in deploy)
*.log
logs/

# If you want to keep seed images, move them to e.g. media_seeds/ and remove from ignore.

# --- Environment & secrets ---
.env
.env.*
!example.env      # Provide a template if created

# --- Editor / IDE ---
.vscode/
.idea/
*.code-workspace

# --- OS junk ---
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Testing / coverage ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
pytestdebug.log

# --- Jupyter / notebooks (if added later) ---
.ipynb_checkpoints/

# --- Tool caches ---
.mypy_cache/
.ruff_cache/
.pytype/
.tox/
/.coverage

# --- Temporary / scratch ---
tmp/
temp/
*.swp
*.swo

# --- Node / front-end (future proofing) ---
node_modules/
*.tsbuildinfo

# --- Backup files ---
*.bak
*.orig
*.rej

# --- Images generated or resized dynamically (if implemented later) ---
processed_images/

# End of file
